
-- term.lua

--# CALL FUNCTIONS: TERM #------------------------------------------------------

-- run command in a new or existing tmux window
local function term_new_window(cmd, error_name)
end

-- run command in an overlay pane
local function term_overlay(cmd, sleep_duration, error_name)
end

-- run command in same window on a new pane
local function term_split_window(cmd, side, error_name)
end

